## Yes, Fly.io is 100% Free for Your Requirements

### Fly.io Free Tier - What You Get

| Resource | Free Allocation |
|----------|-----------------|
| **VMs** | 3 shared VMs |
| **Storage** | 3 GB persistent volumes |
| **Bandwidth** | 160 GB egress/month |
| **Build minutes** | Free (with limitations) |

### Will It Work for Your App?

| Requirement | Fly.io Support |
|-------------|----------------|
| ✅ Always-on (24/7) | Yes - stays running |
| ✅ Scheduler (runs at 3 PM) | Yes - no issues |
| ✅ Telegram bot webhooks | Yes - public URL provided |
| ✅ API calls to Yahoo Finance | Yes - outbound works fine |
| ✅ Python dependencies | Yes - pip install works |

### What You Pay: **$0**

The 3 free VMs are enough for your scanner. As long as your app doesn't exceed:
- 160 GB bandwidth/month
- 3 GB storage

You'll pay **nothing forever** (not just 12 months like AWS/GCP).

---

### Quick Setup (5 minutes)

```bash
# 1. Install Fly CLI
brew install flyctl

# 2. Login
fly auth login

# 3. Launch (creates Dockerfile + config automatically)
fly launch --name stealth-scanner

# 4. Set secrets
fly secrets set TELEGRAM_BOT_TOKEN=your_token
fly secrets set TELEGRAM_CHAT_ID=your_chat_id
# Add API keys as needed (OPENAI_API_KEY, etc.)

# 5. Deploy
fly deploy
```

That's it! You'll get a public URL like `https://stealth-scanner.fly.dev` and your bot will work immediately.

---

### One Caveat

The first time you deploy, it asks for credit card (for identity verification), but **you won't be charged**. You can skip this with:
```bash
fly launch --name your-app --no-deploy
# Then manually set card to "Free" in billing settings
```

**Go ahead - Fly.io is your best free option.**